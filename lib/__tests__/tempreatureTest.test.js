import assert from 'assert';
import tempreatureTest from '../index.js';
import testData from './testData';

describe('tempreatureTest', () => {
  it('has a test', () => {
    assert(false, 'tempreatureTest should have a test');
  });
});
