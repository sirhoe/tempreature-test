import { average, median, mode } from './tempreatureStatistic';
import { readLine } from './tempreaturReader    '

const getAverage = () => {
};

const getMedian = () => {
};

const getMode = () => {
};

const readData = () => {
};

export default 'getAverage, getMedian, getMode';
