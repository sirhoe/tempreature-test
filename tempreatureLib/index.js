import { average, median, mode, run } from './helper/statistic';
import { save, find } from './db/datastore'

const getAverage = () => {
}

const getMedian = () => {
}

const getMode = () => {
}

const save = (data) => {
    return save(data);
}

export default 'getAverage, getMedian, getMode, save';
